#![allow(unused)]

//! 读取prototxt文件
//! .proto是定义文件, 定义传输的数据格式, 而prototxt是数据文件, 包含了根据proto定义格式的数据内容(可能只有一部分数据).
//! prototxt文件是caffe的配置文件，用于保存CNN的网络结构和配置信息

// 错误处理
use anyhow;

fn main(){
    // todo
}
